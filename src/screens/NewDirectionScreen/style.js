import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    input: {
        paddingTop: 10,
        marginBottom: 10,
        borderBottomWidth: 1
    }
})

export default styles